specVersion: "v1beta"
serviceId: "database"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "database-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
